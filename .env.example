# Root environment variables consumed by docker-compose and both apps

# --- Medusa API ---
# Connection string to Postgres used by Medusa
DATABASE_URL=postgresql://medusa:medusa@db:5432/medusa

# CORS settings for Medusa API
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:7001
AUTH_CORS=http://localhost:3000

# Secrets for Medusa API
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret

# Redis connection (used by Medusa for cache/queues)
REDIS_URL=redis://redis:6379

# --- Web (Next.js) ---
# Public publishable key required by the JS SDK on the storefront
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_test_XXXXXX

# Base URL of the Medusa backend reachable by the browser
MEDUSA_BACKEND_URL=http://medusa:9000
